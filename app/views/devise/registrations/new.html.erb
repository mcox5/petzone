<div class="container-fluid">
  <div class="row">
    <div class="mascota col-6">
      <img src="https://bit.ly/3iVr0my" alt="">
    </div>
    <div class="humanos card col-6">

      <h2>Sign up</h2>

      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: :false }) do |f| %>
        <%= f.error_notification %>

        <div class=" card form-inputs m-2 p-3">
          <%= f.input :email,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "email", class: "rounded-4" }%>
          <%= f.input :password,
                      required: true,
                      hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                      input_html: { autocomplete: "new-password", class: "rounded-4" } %>
          <%= f.input :password_confirmation,
                      required: true,
                      input_html: { autocomplete: "new-password", class: "rounded-4" } %>
        </div>

          <%= f.button :submit, "Sign up", class: " cuenta btn-outline-success rounded-4 text-center" %>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
