Aca debe ir el form para crear/editar cita

<%= simple_form_for([pet, appointment]) do |f| %>
  <%= f.input :veterinary_name %>
  <%= f.input :doctor_name %>
  <%= f.input :date %>
  <%= f.submit %>
<% end %>
